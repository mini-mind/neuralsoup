# 模型编辑器完整指南 (已过时)

**注意：此文档描述的是项目重构前的旧版模型编辑器，其内容已不再适用。**

随着项目架构向分层、模块化和事件驱动的模式迁移，SNN（脉冲神经网络）模型编辑器 (`S-NNTopologyEditor`) 需要进行相应的重构，以适配新的核心引擎和服务。

在相关的重构工作完成之前，本文档将暂停更新。

未来的新版指南将包含以下内容：
- 如何在新的UI框架下与核心仿真引擎交互。
- 如何通过全局状态 (`globalState`) 和事件总线 (`globalEventBus`) 管理编辑器状态。
- 新的渲染机制和交互逻辑。

---

*（旧内容已删除，待未来更新）*
